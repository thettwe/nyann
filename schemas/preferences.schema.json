{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nyann.dev/schemas/preferences/v2.json",
  "title": "NyannPreferences",
  "description": "~/.claude/nyann/preferences.json — per-user nyann defaults collected during setup. schemaVersion: 1 (v1.10.0 baseline) or 2 (v1.12.0 with proactive-awareness fields).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1, "maximum": 2 },
    "default_profile": {
      "type": "string",
      "description": "Profile name to use when none specified. 'auto-detect' defers to stack detection.",
      "default": "auto-detect"
    },
    "branching_strategy": {
      "type": "string",
      "enum": ["auto-detect", "github-flow", "gitflow", "trunk-based"],
      "description": "Preferred branching strategy. 'auto-detect' defers to recommend-branch.sh.",
      "default": "auto-detect"
    },
    "commit_format": {
      "type": "string",
      "enum": ["conventional-commits", "custom"],
      "description": "Preferred commit message format.",
      "default": "conventional-commits"
    },
    "gh_integration": {
      "type": "boolean",
      "description": "Whether to enable GitHub CLI integration (branch protection, PR helpers) when gh is available.",
      "default": true
    },
    "documentation_storage": {
      "type": "string",
      "enum": ["local", "obsidian", "notion"],
      "description": "Where to route generated documentation.",
      "default": "local"
    },
    "auto_sync_team_profiles": {
      "type": "boolean",
      "description": "Automatically sync team profile sources during bootstrap when stale.",
      "default": false
    },
    "git_identity": {
      "type": "object",
      "description": "Confirmed git identity (read from `git config` at setup time, user confirmed). Added in schemaVersion 2.",
      "additionalProperties": false,
      "properties": {
        "name":      { "type": "string" },
        "email":     { "type": "string" },
        "confirmed": { "type": "boolean", "default": false }
      }
    },
    "session_triage": {
      "type": "boolean",
      "description": "Run lightweight drift check at session start (P1). Added in schemaVersion 2.",
      "default": true
    },
    "guard_default_severity": {
      "type": "string",
      "enum": ["advisory", "confirm"],
      "description": "Default severity floor for pre-action guards (P2). 'advisory' warns + continues; 'confirm' prompts. Added in schemaVersion 2.",
      "default": "advisory"
    },
    "notifications": {
      "type": "object",
      "description": "Per-channel notification toggles. Added in schemaVersion 2.",
      "additionalProperties": false,
      "properties": {
        "sentinel":          { "type": "boolean", "default": true,  "description": "CI sentinel (P3) state-change notifications." },
        "staleness_alerts":  { "type": "boolean", "default": true,  "description": "Documentation staleness notifications (P4)." }
      }
    },
    "setup_completed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp of when setup was last completed."
    }
  }
}
