# nyann CI workflow — Generic
# Generated by bin/gen-ci.sh from the project's nyann profile.
# Replace the placeholder `exit 1` lines below with real commands.
# These steps deliberately fail until you fill them in — a CI that
# always passes is worse than no CI at all (false-green).

name: CI

on:
  push:
    branches: [${BASE_BRANCHES}]
${PATH_FILTERS}  pull_request:
    branches: [${BASE_BRANCHES}]
${PATH_FILTERS}
jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Lint
        run: |
          echo "Replace this step with your linter (e.g. 'npm run lint')." >&2
          exit 1

      - name: Test
        run: |
          echo "Replace this step with your test command (e.g. 'npm test')." >&2
          exit 1
